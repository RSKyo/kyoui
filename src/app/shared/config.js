export const config = {
  DEBUG:true,
  FIXED: true,
  DEFAULT_DECIMALS: 3,
  PADDING_RATIO: 0.05,
  TOLERANCE: 0.01, // 0.01 UI 坐标对比；0.001~0.005 动画路径连接（如贝塞尔）；
  constants: {
    AXIS: {
      DY: "dy",
      DX: "dx",
    },
  },
};
